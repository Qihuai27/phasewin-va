# Config Directory

Put model config files here.

Expected file for the current unified detection task:

```text
config/
  GroundingDINO_SwinT_OGC.py
```

This file exists in the original `../phasewin-search/config/` tree.
